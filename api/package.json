{
  "name": "qagibank-api-playwright",
  "version": "1.0.0",
  "private": true,
  "description": "Automacao de testes de API da Dog CEO API com Playwright.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
