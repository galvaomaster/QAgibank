{
  "name": "qagibank-playwright",
  "version": "1.0.0",
  "private": true,
  "description": "Automacao de testes da busca do Blog do Agi com Playwright.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
